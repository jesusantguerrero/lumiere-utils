{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna publish --conventional-commits --yes",
    "lerna-publish": "lerna publish --conventional-commits --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
